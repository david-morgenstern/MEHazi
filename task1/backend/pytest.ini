[pytest]
# Run pytest from this directory. `pythonpath = .` puts backend/ on sys.path so
# the tests import `pipeline` the same way the container does, with no install
# step and no packaging boilerplate.
pythonpath = .
testpaths = tests
